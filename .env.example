# コピーして `opera_ws/.env` として保存し、値を書き換えてください。
# `.env` は .gitignore 対象です。

IPRO_CAMERA_IP=192.168.11.200
IPRO_CAMERA_USER=username
IPRO_CAMERA_PASSWORD=password

# 任意: .env の場所を固定したい場合（ノード起動前に export）
# export IPRO_ENV_FILE=/home/you/opera_ws/.env
